/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Możesz tu dodać inne konfiguracje, np. dla obrazów:
  // images: {
  //   domains: ['example.com'], // Dodaj domeny, z których ładujesz obrazy
  // },
  // Jeśli używasz niestandardowego serwera, ustaw to na true:
  // experimental: {
  //   serverActions: true, // Jeśli planujesz używać Server Actions
  // },
  // Konfiguracja dla i18n, jeśli potrzebna
  // i18n: {
  //   locales: ['en', 'pl'],
  //   defaultLocale: 'pl',
  // },
};

export default nextConfig; 